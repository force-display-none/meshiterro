<section class="sct-color-1 slice">
  <div class="container-fluid masonry-container">
    <div class="row">
      <div class="col-md-8 col-sm-offset-2">
        <div class="masonry-wrapper-cols">
          <div class="masonry-gutter"></div>

         <!-- showアクションに@post_imagesを渡していないのでアクションに追加記述して下さい！ -->
         <% post_images.each do |post_image| %>
         <!-- コントローラーから渡されたpost_images変数に入っているハッシュから1つづつ取り出してpost_image変数へ格納します -->
            <div class="masonry-block">
              <div class="block block-image v1">
                <div class="block-image">
                  <div class="view view-first">
                    <%= link_to post_image_path(post_image.id) do %>
                    <%= attachment_image_tag post_image, :image %>
                    <!-- attachment_image_tagはrefileの<img>タグを作成してくれるヘルパーです。この場合、post_imageモデルの中のimage_idを指定するとrefileが自動的に解釈して保存された画像へのimgタグを生成します -->
                    <% end %>
                  </div>
                </div>
                <div class="block-content">
                  <!-- プロフィール画像が反映されるべきimage_tagを対応したattachment_image_tagに変えています -->
                  <%= attachment_image_tag post_image.user, :profile_image, class: "img-circle pull-left profile-thumb" %>
                  <h3 class="block-title">
                   <%= link_to post_image_path(post_image.id) do %>
                     <i class="fa fa-map-marker"></i><%= post_image.shop_name %>
                                                     <!-- post_imageモデルに定義されているshop_nameカラムを表示します -->
                   <% end %>
                  </h3>
                  <p><%= post_image.caption %></p>
                     <!-- post_imageモデルに定義されているcaptionカラムを表示します -->
                  <ul class="inline-meta">
                    <li>By
                      <%= post_image.user.name %>
                      <!-- post_imageモデルに定義されているuser_idに関連付いたuserモデルのnameカラムを表示します -->
                    </li>
                    <li>
                    <a href="#">23 コメント</a>
                    </li>
                  </ul>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
<%= paginate post_images, class: "pagenate" %>
</section>